.onLoad <- function(libname, pkgname){
    options(scribd_api_key = NULL)      # API Key
    options(scribd_secret_key = NULL)   # API Key
    options(scribd_session_key = NULL)  # Session Key
    options(scribd_user = NULL)         # Temporary User
}
